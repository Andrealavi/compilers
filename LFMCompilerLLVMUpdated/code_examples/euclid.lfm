// This is an .lfm file for the euclid's algorithm
function euclid(x y)
   if y==0 : x;
      true : let z = x%y in
                 euclid(y,z)
             end
   end
end
function main() /* This is a function */
   euclid(20,45)
end
